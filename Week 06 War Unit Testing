const expect = chai.expect
const assert = chai.assert

describe(`Determine Round Winner`, () =>{
  
  it(`#Should return Player 1 Wins Game!!!`, () => {
    expect(scoreDisplay()).to.equal(console.log("Player 1 Wins Game!!!"))
    })

  it(`#Should return Player 1 Wins Game!!!`, () => {
    expect(scoreDisplay()).to.equal(console.log("Player 1 Wins Game!!!"))
    })

})

